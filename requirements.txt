# FLARE runtime dependencies (training + inference).
# Install the package instead with:  pip install -e .
# and the PE extras with:            pip install -e ".[estimator]"

# Core scientific computing
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.2.0

# Deep learning
torch>=2.0.0

# Gravitational-wave waveform generation
pycbc>=2.0.0
lalsuite>=7.0            # LIGO Analysis Library Suite (SEOBNRv4 / SEOBNRv4HM)

# Hyperparameter optimisation
optuna>=3.0.0

# Visualisation
matplotlib>=3.7.0

# Utilities
tqdm>=4.64.0
pyyaml>=6.0
requests>=2.28.0
psutil>=5.9.0

# --- Parameter estimation extras (scripts/pe_benchmark.py, hm_pe_*.py) ---
# gwpy>=3.0.0
# gwosc>=0.7.0
# emcee>=3.0.0
# dynesty>=2.1.0
# corner>=2.2.0
# h5py>=3.0.0
# astropy>=5.0.0

# System notes:
# - CUDA: install a CUDA build of PyTorch from https://pytorch.org/ for GPU training.
# - PyCBC pulls in LALSuite, which may require system libraries on Linux.
